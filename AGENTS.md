# 博客写作与发布规范

## 读者与表达

- 技术博客默认面向已经熟悉相应研究方向的读者。压缩基础知识，解释关键机制、公式和设计取舍；默认不扩展为代码复现教程。
- 写作前明确文章解决的具体问题和贯穿全文的论证顺序。目录随内容调整，不套用统一章节模板。
- 保持已有标题与永久链接稳定，除非用户要求更改。小标题直接命名技术主题，避免“怎么看”“记住什么”“一文读懂”等导读口号。
- 用自然段落讲解，交代设计依赖：前面的方案留下什么问题，后续模块怎样处理。不要拼接模块介绍或图注。
- 文字简洁，但不以固定字数删掉必要推导。避免每节重复相同句式、强行总结、机械分成若干标签，或追加与当前讨论无关的泛泛局限。

- 示意图必须直接呈现用户想比较的对象：分箱就画同尺度距离区间，采样就画空间位置。不能用模块方框与箭头流程图代替几何、数值或方法差异的示意。简化参数需明确标注，与真实论文配置区分。
- 流程、数据变化和配置差异优先用简洁示意图解释，正文只保留理解图示所必需的说明；不要每次追问都追加大段文字。原论文图表保留，补充图明确标为自绘，未证实的步骤标明未知。
- 代码、逐步实现和推导较长时放入可展开区域，正文保留主线和关键结论。

- 用户对某处提出的表达偏好适用于同类内容：修改一项消融时主动检查其他消融。每组消融应有直接呈现实际改动的补充示意，原表提供数据；绘图后删减重复文字，不能只在用户点名处修补。

## 论文解读

- 以研究问题、方法设计和实验证据组织文章。关键消融紧跟相关模块，总体性能与扩展实验放在后面；这是组织原则，不是所有论文都必须使用的目录。
- 按用户已确定的形式使用原论文图表截图。全部展示并解释，保留原编号、论文版本、来源页码和清晰裁切，支持点击放大；允许按论述需要调整顺序。
- 用户追问“具体怎么做”时，必须解释输入标注、目标张量构造、网络输出、loss 和梯度路径，以及切换配置时需要更改的步骤；用小数值例子或伪代码说明，不能用展开缩写代替实现解释。默认实现与未公开的消融实现必须分开陈述。
- 解释操作顺序消融时，必须画出各步的输入、更新对象和中间状态，说明后一步接收前一步的输出；区分同名注意力所在模块，以及当前层新信息与上一层继承信息。不能只排列模块缩写或把顺序依赖解释成已证实的性能原因。
- 解释消融实验时，必须先指出被改动的模块或训练目标，展开行名缩写，说明每行修改什么、保留什么，以及哪些行构成单变量对照；明确结果列衡量的任务。不能只报数值和优劣。原文未说明的标签来源、处理流程或实现细节必须标明，不自行补全。
- 图表应支撑具体论点。正文说明与当前问题有关的模块、对比和数值，不逐项复述所有单元格，也不要求每张图表单独占一节。
- 即使暂时隐藏图片，正文也应具有完整逻辑；查看图表时，应能找到对应解释与实验条件。
- 关键公式解释符号、作用及其在网络中的位置，区分训练与推理。核对原论文，不凭记忆补齐公式。
- 区分论文报告、论文作者解释与博客分析。额外引用官方代码时明确注明来源，不把当前代码行为直接归为论文原始实验配置。
- 不把注意力可视化当作因果证明，不把消融收益泛化到所有设置。比较结果时核对数据划分、类别、指标、阈值和额外监督；原文数值冲突应保留差异，不自行改数。
- 不虚构作者经历、复现结果或实验图。局限应针对证据实际未回答的问题。

## MonoDETR 的组织示例

这是一篇文章的具体安排，不是其他论文的固定模板：

1. 研究动机与方法定位：中心引导的局限和全局深度线索，图 1、图 2、表 1。
2. 整体架构：视觉、深度、查询的信息流，图 4。
3. 前景深度预测与编码：监督、LID、深度编码器、逐米位置编码，图 3、表 8、表 6、表 9。
4. 深度引导解码与三维预测：D → I → V、深度计算、预测头、匹配和训练，表 7、图 6，并引用图 4。
5. 检测效果与计算开销：模块收益、KITTI 精度、推理效率，表 5、表 2、表 3。
6. 多视角扩展与讨论：PETRv2 和 BEVFormer 的接入与效果、证据边界，图 5、表 4。

## 检查与发布

### 数学排版与重点

- 数学表达必须使用 LaTeX 经 KaTeX 正式渲染，包含分式、根号、求和范围、上下标和多行对齐；禁止用纯文本、Unicode 下标或手工 HTML 拼出展示公式。
- 本仓库使用 独占行的 `$$ ... $$` 表示独立公式，`$ ... $` 表示行内公式。公式在构建时渲染，样式与字体随站点部署，不依赖访问者临时加载外部数学服务。
- 指标与变量统一数学记法，例如 `\mathrm{AP}_{3D}`、`\mathrm{AP}_{\mathrm{BEV}}`。不允许网页显示裸露的 LaTeX 转义字符或丢失下标。
- 正文必须有可扫描的重点：用少量加粗标出核心机制、设计选择和直接支持它的实验结论。避免整段加粗、每句加粗，或为了强调而恢复模板化卡片。
- 验收时检查正式数学渲染节点、公式错误、字体资源、行内基线与窄屏溢出；检查重点能否串起论述，而不是只突出数字。

- 检查图表覆盖与引用顺序、技术准确性、正文衔接、标题层级、公式显示和放大链接。
- 检查桌面与窄屏排版，图片不能裁掉内容，长公式不能撑破页面。若浏览器检查工具不可用，明确记录未完成的视觉检查，不以构建通过代替。
- 运行 `npm run build`，确认文章和资源生成正常。只提交本次相关源文件，不提交 `public`、本地论文 PDF、临时脚本或私有笔记。
- 用户已要求发布的更新，提交并推送至现有仓库；发布后检查原网址与图片资源。未获得发布指示时，不自行扩大为发布任务。
